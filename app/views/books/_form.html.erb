<!-- filepath: /home/csiebel/dev/Ruby/Rails/bookshelf/app/views/books/_form.html.erb -->
<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "Fehler") %> verhinderten das Speichern:</h2>
      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, "Titel" %><br>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :author, "Autor" %><br>
    <%= form.text_field :author %>
  </div>
  <div>
    <%= form.label :description, "Beschreibung" %><br>
    <%= form.text_area :description %>
  </div>
  <div>
    <%= form.label :published_on, "Veröffentlicht am" %><br>
    <%= form.date_select :published_on %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
